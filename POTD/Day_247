class Solution:
    def maxSubarrayXOR(self, arr, k):
        x=0
        n=len(arr)
        ans=0
        for i in range(k-1):
            x ^= arr[i]
        for i in range(k-1,n):
            x ^= arr[i]
            ans=max(ans,x)
            x ^= arr[i-k+1]
        return ans
