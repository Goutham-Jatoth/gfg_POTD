 def maxSum(self, arr):
        n = len(arr)
        base_sum = sum(arr)
        max_sum = curr_sum = sum(i * arr[i] for i in range(1, n))
        for a in arr:
            curr_sum += n * a - base_sum
            if curr_sum > max_sum:
                max_sum = curr_sum
        return max_sum
