class Solution:
	def countStrings(self,n):
    	odds = 1
    	evens = 1
    	for _ in range(1,n):
    	    prev_sum = odds + evens
    	    odds = evens
    	    evens = prev_sum
        return odds + evens
