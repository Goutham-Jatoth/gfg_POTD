def hIndex(self, citations):
        n = len(citations)
        freqs = [0] * (n + 1)
        for c in citations:
            freqs[min(c, n)] += 1
        count = 0
        for h in reversed(range(n + 1)):
            count += freqs[h]
            if count >= h:
                return h
        return 0
